<Window x:Class="HTrack.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Habit Tracker"
        Height="200"
        Width="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="False"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Left="50"
        Top="50">
    
    <Border Background="{StaticResource BackgroundBrush}" 
            CornerRadius="8" 
            BorderBrush="{StaticResource OutlineBrush}" 
            BorderThickness="1"
            Opacity="0.95"
            MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <TextBlock Grid.Row="0" 
                       Text="Habit Tracker" 
                       FontSize="16" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,10"
                       Foreground="#333"/>
            
            <!-- Habit Grid Container -->
            <ScrollViewer Grid.Row="1" 
                          HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Hidden">
                <StackPanel Name="HabitGridContainer" Orientation="Vertical">
                    <!-- Day Labels -->
                    <Grid Name="DayLabelsGrid" Margin="0,0,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="14"/>
                            <ColumnDefinition Width="14"/>
                            <ColumnDefinition Width="14"/>
                            <ColumnDefinition Width="14"/>
                            <ColumnDefinition Width="14"/>
                            <ColumnDefinition Width="14"/>
                            <ColumnDefinition Width="14"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="1" Text="M" FontSize="8" HorizontalAlignment="Center" Foreground="#666"/>
                        <TextBlock Grid.Column="3" Text="W" FontSize="8" HorizontalAlignment="Center" Foreground="#666"/>
                        <TextBlock Grid.Column="5" Text="F" FontSize="8" HorizontalAlignment="Center" Foreground="#666"/>
                    </Grid>
                    
                    <!-- Main Habit Grid -->
                    <UniformGrid Name="HabitGrid" Rows="7" Margin="20,0,0,0"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window> 